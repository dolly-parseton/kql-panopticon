# Threat Enrichment Pack
#
# Demonstrates foreach iteration with HTTP enrichment.
# Queries for suspicious IPs, then enriches each with threat intelligence.
#
# Usage:
#   pack-runner examples/threat-enrichment.yaml
#   pack-runner examples/threat-enrichment.yaml --input min_failed_logins=10

name: Threat Enrichment
description: |
  Identify suspicious IPs from sign-in failures and enrich with
  threat intelligence via external API.
version: "1.0"

acquisition:
  inputs:
    - name: min_failed_logins
      label: Minimum Failed Logins
      description: Threshold for considering an IP suspicious
      required: false
      default: "5"
      example: "5"

  secrets:
    # API key from environment variable (optional)
    threat_api_key: ${THREAT_INTEL_API_KEY}

  steps:
    # Step 1: Find IPs with high failed login counts
    - name: suspicious_ips
      type: kql
      query: |
        SigninLogs
        | where TimeGenerated > ago(7d)
        | where ResultType != "0"
        | summarize
            FailedAttempts = count(),
            TargetedUsers = dcount(UserPrincipalName),
            UserList = make_set(UserPrincipalName, 5),
            Countries = make_set(Location, 3),
            FirstSeen = min(TimeGenerated),
            LastSeen = max(TimeGenerated)
          by IPAddress
        | where FailedAttempts >= {{inputs.min_failed_logins}}
        | order by FailedAttempts desc
        | take 20
      timespan: P7D

    # Step 2: Enrich each suspicious IP with threat intel
    # This demonstrates foreach iteration over step results
    - name: ip_enrichment
      type: http
      depends_on: [suspicious_ips]
      foreach: "suspicious_ips as ip"
      request:
        method: GET
        url: "https://api.abuseipdb.com/api/v2/check"
        params:
          ipAddress: "{{ip.IPAddress}}"
          maxAgeInDays: "90"
        headers:
          Key: "{{secrets.threat_api_key}}"
          Accept: "application/json"
      response:
        fields:
          ip_address: "$.data.ipAddress"
          is_public: "$.data.isPublic"
          abuse_confidence: "$.data.abuseConfidenceScore"
          country_code: "$.data.countryCode"
          isp: "$.data.isp"
          domain: "$.data.domain"
          total_reports: "$.data.totalReports"
          last_reported: "$.data.lastReportedAt"
      rate_limit:
        requests: 10
        per: second
      aggregate: append
      on_error: continue
      on_empty: skip

    # Step 3: Combine findings
    - name: enriched_results
      type: kql
      depends_on: [suspicious_ips, ip_enrichment]
      when: "ip_enrichment is not empty"
      query: |
        // Join original findings with enrichment data
        let original = datatable(
          IPAddress: string,
          FailedAttempts: int,
          TargetedUsers: int
        ) [
          {{suspicious_ips.*.IPAddress}}, {{suspicious_ips.*.FailedAttempts}}, {{suspicious_ips.*.TargetedUsers}}
        ];
        let enriched = datatable(
          ip_address: string,
          abuse_confidence: int,
          total_reports: int,
          isp: string
        ) [
          {{ip_enrichment.*.ip_address}}, {{ip_enrichment.*.abuse_confidence}}, {{ip_enrichment.*.total_reports}}, {{ip_enrichment.*.isp}}
        ];
        original
        | join kind=leftouter enriched on $left.IPAddress == $right.ip_address
        | project
            IPAddress,
            FailedAttempts,
            TargetedUsers,
            AbuseScore = coalesce(abuse_confidence, 0),
            TotalReports = coalesce(total_reports, 0),
            ISP = coalesce(isp, "Unknown")
        | order by AbuseScore desc, FailedAttempts desc
      timespan: P7D
      on_error: continue

# Processing configuration - runs per workspace after acquisition
processing:
  steps:
    - name: risk_score
      type: scoring
      indicators:
        - name: high_abuse_score
          condition: "enriched_results.any(AbuseScore > 75)"
          weight: 30
          description: IP has high abuse confidence score

        - name: many_reports
          condition: "enriched_results.any(total_reports > 100)"
          weight: 20
          description: IP has many abuse reports

        - name: high_failed_logins
          condition: "suspicious_ips.any(FailedAttempts > 50)"
          weight: 25
          description: High volume of failed login attempts

        - name: multiple_targets
          condition: "suspicious_ips.any(TargetedUsers > 10)"
          weight: 15
          description: Multiple users targeted by same IP

      thresholds:
        - level: CRITICAL
          min_score: 60
          summary: Critical threat indicators detected
          recommendation: Immediate investigation and IP blocking recommended

        - level: HIGH
          min_score: 40
          summary: Significant threat indicators detected
          recommendation: Review affected accounts and consider blocking

        - level: MEDIUM
          min_score: 20
          summary: Moderate threat indicators detected
          recommendation: Monitor affected IPs and users

        - level: LOW
          min_score: 0
          summary: Low risk detected
          recommendation: Continue standard monitoring

# Report definitions - generated after processing
reporting:
  reports:
    - name: threat-report
      format: markdown
      output: "threat-report.md"
      template: |
        # Threat Enrichment Report

        Generated: {{ meta.timestamp }}

        ## Summary

        - Suspicious IPs found: {{ suspicious_ips | length }}
        - IPs enriched: {{ ip_enrichment | length }}

        ## Risk Assessment

        **Score:** {{ processing.risk_score.score }}
        **Level:** {{ processing.risk_score.level }}

        {{ processing.risk_score.summary }}

        ## High-Risk IPs

        {% for ip in enriched_results %}
        {% if ip.AbuseScore > 50 %}
        - **{{ ip.IPAddress }}**: Abuse Score {{ ip.AbuseScore }}, {{ ip.FailedAttempts }} failed logins ({{ ip.ISP }})
        {% endif %}
        {% endfor %}

        ## Matched Indicators

        {% for indicator in processing.risk_score.matched_indicators %}
        - {{ indicator.name }} (+{{ indicator.weight }})
        {% endfor %}

        ## Recommendations

        {{ processing.risk_score.recommendation }}
